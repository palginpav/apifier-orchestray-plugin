# Generated by apifier 0.0.1 on 2026-05-19T07:42:11.034Z from https://widget.example.com/openapi.json.
# Mapping schema_version: 1. Do not edit by hand.
# Source: https://widget.example.com/openapi.json
openapi: 3.1.0
info:
  title: Widgets API
  version: "2026-04-12"
  description: REST API for managing widgets.
servers:
  - url: https://api.widget.example.com/v1
    description: production
paths:
  /widgets:
    post:
      operationId: createWidget
      summary: Create a widget.
      description: Creates a new widget owned by the authenticated user.
      tags:
        - widgets
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WidgetCreate"
      responses:
        "201":
          description: Created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Widget"
        "400":
          description: Validation failure.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      security:
        - bearer-jwt: []
  /widgets/{id}:
    get:
      operationId: getWidget
      summary: Get a widget by id.
      tags:
        - widgets
      parameters:
        - name: id
          in: path
          required: true
          description: Widget id.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Widget"
        "404":
          description: Not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
      security:
        - bearer-jwt: []
components:
  schemas:
    ApiError:
      type: object
      description: Standard error envelope.
      properties:
        code:
          type: string
          description: Stable machine-readable error code.
        message:
          type: string
          description: Human-readable message.
      required:
        - code
        - message
    Widget:
      type: object
      description: A widget.
      properties:
        id:
          type: string
          format: uuid
          description: Widget identifier.
        color:
          type: string
          description: Widget color.
          enum:
            - red
            - green
            - blue
        size:
          type: integer
          format: int32
          description: Widget size in mm.
      required:
        - id
        - color
    WidgetCreate:
      type: object
      description: Payload to create a widget.
      properties:
        color:
          type: string
          enum:
            - red
            - green
            - blue
        size:
          type: integer
          format: int32
      required:
        - color
  securitySchemes:
    bearer-jwt:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT issued by /oauth/token.
